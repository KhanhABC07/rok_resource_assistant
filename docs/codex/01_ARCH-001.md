# ARCH-001 — Modularize the current codebase

Use together with `00_SHARED_CONTEXT.md`. Implement only this task.

## Codex Prompt

```text
Refactor the existing project into clear presentation, application, domain, automation, and adapters boundaries without changing user-visible behavior. Start by extracting business logic from `gui/task_queue.py` and `gui/automation.py` into application services and view models. Create a composition root in `app.py`, define Protocol-based ports, and remove direct repository/emulator access from Qt widgets. Keep compatibility imports where needed. Add architecture tests that prevent domain modules from importing PyQt6, SQLite, OpenCV, or emulator-specific modules. Update `docs/architecture.md` with dependency rules and an incremental migration map.
```
