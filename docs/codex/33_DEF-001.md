# DEF-001 — Enable Peace Shield on Incoming Attack

Use together with `00_SHARED_CONTEXT.md`. Implement only this task.

## Codex Prompt

```text
Implement a high-priority defensive policy workflow. Add a lightweight incoming-attack monitor with debounce and cooldown. When the configured attack signal is detected, enqueue/preempt with an emergency job, verify the active character/city state, evaluate shield policy and allowed item durations, navigate to the allowed shield item/buff, apply it, and verify the shield-active postcondition. If eligibility, inventory, or postcondition fails, create a critical incident and notify the operator. Enforce explicit spend limits and manual override. Add tests for preemption, duplicate signals, no shield item, policy denial, successful activation, and false-positive detection.
```
