# CITY-001 — Collect City Resources

Use together with `00_SHARED_CONTEXT.md`. Implement only this task.

## Codex Prompt

```text
Implement idempotent city resource collection. Define layout profiles and semantic ROIs for food, wood, stone, gold, and optional crystal. Normalize to CITY_HOME, detect ready collection indicators only inside configured ROIs, click each at most once per pass, and verify the indicator or resource bubble changed. Bound the number of passes. Report collected resource types. Add replay tests for multiple layouts, partial readiness, overlapping icons, popup overlay, and no resources ready.
```
