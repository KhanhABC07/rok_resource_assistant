# ALL-004 — Press Help Button

Use together with `00_SHARED_CONTEXT.md`. Implement only this task.

## Codex Prompt

```text
Implement the alliance help action as a small idempotent workflow. Normalize to a scene where the help indicator is observable, detect the semantic `alliance.help.ready` template, click once, and verify the badge disappears or the button becomes inactive. Return SKIPPED if no help is available. Add replay tests for ready, not ready, overlay obstruction, and postcondition failure.
```
