# INV-001 — Use Items

Use together with `00_SHARED_CONTEXT.md`. Implement only this task.

## Codex Prompt

```text
Implement inventory item use with deny-by-default safety. Add a whitelist keyed by semantic item type, allowed quantity per run/day, total budget, and dry-run preview. Navigate to inventory, find only whitelisted items, verify item identity and available quantity, enter the configured amount, confirm, and verify inventory delta. Block premium/rare/unrecognized items. Add unit tests for budgets and replay tests for item found, insufficient quantity, wrong item, confirmation, and inventory-delta failure.
```
