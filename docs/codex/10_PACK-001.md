# PACK-001 — Windows CI, packaging, and release channels

Use together with `00_SHARED_CONTEXT.md`. Implement only this task.

## Codex Prompt

```text
Create a Windows-first CI pipeline that installs dependencies from a locked file, runs unit/integration/PyQt offscreen tests, builds the desktop application, validates database migrations, and validates template packs. Produce a versioned artifact with checksums and an SBOM. Separate application version from template-pack version. Implement staging/canary/stable release channels and a rollback procedure. Do not bundle the development virtual environment or runtime screenshots/database into the release.
```
