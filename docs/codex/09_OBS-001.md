# OBS-001 — Observability and incidents

Use together with `00_SHARED_CONTEXT.md`. Implement only this task.

## Codex Prompt

```text
Add structured JSON logging with correlation fields: job_id, run_id, step_id, instance_id, account_id, character_id, feature_key, workflow_version, and template_pack_version. Persist job/step durations, outcomes, confidence, retries, and evidence references. Implement incident creation for identity mismatch, verification screen, unknown critical popup, recovery exhaustion, and budget denial. Add configurable screenshot retention and a support-bundle exporter that redacts secrets. Extend the dashboard with success rate, blocked/retry counts, queue depth, active jobs, concurrency usage, and recent incidents.
```
