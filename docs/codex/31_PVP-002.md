# PVP-002 — Fight Lost Canyon

Use together with `00_SHARED_CONTEXT.md`. Implement only this task.

## Codex Prompt

```text
Implement Lost Canyon runs using the same reusable canyon domain services but separate semantic templates and workflow definition. Verify feature availability and remaining attempts, choose an opponent by policy, fight, process result, and stop at the configured limit. Keep formation changes disabled by default. Add replay tests for unavailable mode, no attempts, victory/defeat, and UI variations.
```
