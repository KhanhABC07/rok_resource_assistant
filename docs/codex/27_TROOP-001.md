# TROOP-001 — Train Troops

Use together with `00_SHARED_CONTEXT.md`. Implement only this task.

## Codex Prompt

```text
Implement troop training for infantry, archers, cavalry, and siege. Add per-building enabled flags, desired training tier, queue policy, and default no-speedup/no-premium-spend rules. For each enabled building, normalize city view, open the building, verify the queue is free, select the configured tier, start training, and verify the queue timer/state. Classify insufficient resources and busy queue without unsafe retries. Add replay tests per building and tier plus policy validation tests.
```
