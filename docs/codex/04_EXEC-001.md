# EXEC-001 — Workflow engine v2

Use together with `00_SHARED_CONTEXT.md`. Implement only this task.

## Codex Prompt

```text
Design and implement Workflow Engine v2. Introduce typed action and condition registries, workflow definitions with schema versioning, execution context, cancellation tokens, deadlines, step budgets, retry policies, and postcondition verification. Replace boolean-only results with SUCCESS, SKIPPED, BLOCKED, RETRYABLE_FAILURE, FATAL_FAILURE, and CANCELLED. Support sequence, bounded repeat, if/else, wait, click semantic template, tap, swipe, delay, normalize scene, and sub-workflow calls. Persist job and step runs. Provide an adapter that can execute existing automation_tasks/automation_task_steps. Add exhaustive validation and execution tests, including malformed workflows, cancellation, nested branches, timeout, and restart recovery.
```
