# MAP-001 — Explore Fog

Use together with `00_SHARED_CONTEXT.md`. Implement only this task.

## Codex Prompt

```text
Implement bounded fog exploration. Add scout policy, maximum scans, deadline, and optional whitelisted cave/village investigation. Verify an idle scout, locate a valid fog target using map analysis, send the scout, and verify the scout becomes busy. Return BLOCKED when no scout is available and RETRYABLE_FAILURE when no target is found within the budget. Persist ETA when observable. Add replay tests for idle/busy scouts, target found/not found, cave/village prompt, and search budget exhaustion.
```
