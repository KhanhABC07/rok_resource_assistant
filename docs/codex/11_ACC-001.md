# ACC-001 — Switch Accounts

Use together with `00_SHARED_CONTEXT.md`. Implement only this task.

## Codex Prompt

```text
Implement verified account switching for up to six configured game accounts per operator. Add account configuration UI, game_accounts repository support, secret_ref integration, and an AccountSwitchWorkflow. The workflow must validate the target instance session, normalize to a known scene, navigate to account settings, choose the configured login method/account, retrieve secrets only when required, wait for game load, and verify an account fingerprint before updating instance_session. On mismatch, verification/CAPTCHA, unknown confirmation, or timeout, stop and create an incident. Add replay fixtures for already-active account, successful switch, wrong account, login failure, and verification screen. Acceptance: no secret in SQLite/logs; session state changes only after postcondition verification; audit before/after account IDs.
```
