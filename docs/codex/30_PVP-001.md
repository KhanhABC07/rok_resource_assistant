# PVP-001 — Fight Sunset Canyon

Use together with `00_SHARED_CONTEXT.md`. Implement only this task.

## Codex Prompt

```text
Implement Sunset Canyon runs with conservative policy controls. Navigate to Campaign > Sunset Canyon, verify remaining attempts, choose an opponent using a simple configurable rule, start battle, optionally use the existing skip-battle option only when enabled, collect the result, and repeat up to the attempt/policy limit. Do not mutate formations unless explicitly configured. Record opponent metadata if observable, result, and remaining attempts. Add replay tests for no attempts, victory, defeat, result popup, and navigation failure.
```
