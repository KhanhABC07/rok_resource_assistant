# RES-001 — Multi-instance Gather Resources

Use together with `00_SHARED_CONTEXT.md`. Implement only this task.

## Codex Prompt

```text
Implement natural resource gathering for Food, Wood, Stone, and Gold across multiple emulator instances. Add per-character resource policy, preferred levels, bounded fallback levels, march presets, cooldown, and scheduling configuration. The workflow must validate the target instance/character session, verify a free march, normalize to world map, open resource search, select resource/level, validate a found node, click Gather, select the configured march preset, dispatch, and verify a marching postcondition. Return BLOCKED for no free march and RETRYABLE_FAILURE for no node. Persist march ETA when observable. Add replay and scheduler-policy tests covering configured concurrency, scene/session drift, no free march, and safe restart reconciliation.
```
