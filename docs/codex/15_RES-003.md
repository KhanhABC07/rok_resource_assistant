# RES-003 — Gather Gems

Use together with `00_SHARED_CONTEXT.md`. Implement only this task.

## Codex Prompt

```text
Implement bounded gem gathering without CAPTCHA solving or anti-detection behavior. Create a GemSearchPolicy with allowed levels, search radius/attempt limit, total deadline, march preset, and minimum detector confidence. Use a detector or robust feature matching for gem deposits; do not click below threshold. Validate that the node is available, dispatch an available march, and verify marching. Record search attempts and confidence. Add a labeled positive/negative dataset, precision/recall tests, no-node timeout tests, occupied-node tests, and verification-screen hard-stop tests.
```
