# SEC-001 — Secrets and credential references

Use together with `00_SHARED_CONTEXT.md`. Implement only this task.

## Codex Prompt

```text
Add a SecretStore port with a Windows Credential Manager or DPAPI-backed adapter and an in-memory test adapter. Change game_accounts to store only a secret_ref. Add redaction helpers for logs, exceptions, exports, and support bundles. Provide a migration/import flow that allows the operator to move existing credentials into the secret store without writing them to SQLite. Add tests that scan logs and exported JSON to confirm secrets are absent.
```
