# DATA-001 — Database v2 and migrations

Use together with `00_SHARED_CONTEXT.md`. Implement only this task.

## Codex Prompt

```text
Implement a versioned database migration system for the current SQLite database. Add tables for game_accounts, instance_sessions, automation_profiles, feature_configs, schedule_definitions, workflow_definitions, workflow_steps, jobs, job_runs, step_runs, template_packs, templates, screen_observations, incidents, and audit_logs. Migrate existing characters.account_name into a game_accounts relationship without losing data. Add idempotency and useful indexes. Enable foreign keys and WAL safely. Create backup-before-migration and rollback/error handling. Add migration tests using a copy of the current schema and repository tests for all new aggregates.
```
